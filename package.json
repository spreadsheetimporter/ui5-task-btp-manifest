{
  "name": "ui5-task-btp-manifest",
  "version": "0.2.2",
  "description": "custom ui5 tooling task",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local-release": "npx release-it --no-git.requireCleanWorkingDir --no-npm.publish --no-github.release",
    "release": "release-it",
    "build-test": "npm run build --workspace=test",
    "build-test:cf": "npm run build:cf --workspace=test",
    "start:testapp": "npm run start-noflp --workspace=test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/spreadsheetimporter/ui5-task-btp-manifest.git"
  },
  "author": "Marian Zeis",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/spreadsheetimporter/ui5-task-btp-manifest/issues"
  },
  "homepage": "https://github.com/spreadsheetimporter/ui5-task-btp-manifest#readme",
  "devDependencies": {
    "release-it": "^17.4.1",
    "@release-it/conventional-changelog": "^8.0.1"
  },
  "workspaces": [
    "test"
  ]
}
